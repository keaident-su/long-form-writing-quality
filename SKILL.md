---
name: long-form-writing-quality
description: 长文本写作质量控制 Skill。防止超长篇文本（剧本/小说/报告，单篇>5000字）的长文本退化——碎片化断句、机械重复、逗号插入、对话僵硬、剧情重复、时间线矛盾、年龄错误、情节注水凑字数。提供分批生成工作流、5个硬闸门、自动校验脚本、情节级重复检测。
---

# 长文本写作质量控制 Skill

版本：6.0.0

## 适用范围

适用于所有超过5000字的连续文本创作（剧本、小说、报告、长文档）。核心规则通用；标注"剧本专用"的仅剧本启用。

## 五条铁律（违反任何一条 = 重写，不接受讨论）

1. **时间戳严格单调递增**：任何场次的时间不得早于前一场。补充场时间必须在同号主场次之后、下一主场次之前。
2. **同一情节只写一场**：买显示器、装空调、买工作站、给某人报喜、进旧房间——这类"事件性情节"，全片只写1场。需要多角度时用补充场，但补充场必须提供**新信息**（新人物视角/新发现/新转折），不能重复同一动作。
3. **凑字数时禁止重复已有情节**：字数不够时，必须补充**新情节类型**（新场景/新人物/新冲突），不得把已有情节换皮重写。
4. **中文段落禁止 ASCII 双引号**：所有对话引号必须用中文弯引号（"…"，U+201C/U+201D）。禁止用 `"`（U+0022）。
5. **主场次不得随意删除**：删除主场次后必须重新编号，不得出现 341→343 的断裂。补充场必须紧跟同号主场次。

## 五个硬闸门（每批写完必须跑脚本，不过就不交付）

### 闸门1：退化模式扫描

跑 `detect_degradation(text)`：
- 主语后插逗号（"裴砚，说"）
- 碎片句（一行>4个逗号且<50字）
- 对话标签僵硬（"XX，说："）
- 连续5段相同句式开头
- 连续>2个空行

### 闸门2：时间线与场次结构

跑 `verify_scene_order(text)`：
- 所有时间戳严格递增
- 主场次编号连续（无跳号、无重号）
- 补充场紧跟同号主场次
- 补充场时间夹在主场次与下一主场次之间

### 闸门3：情节级重复扫描（v6.0 新增，最重要）

跑 `plot_cluster_scan(text)`：
- 按情节关键词聚类（显示器/空调/工作站/黑客报喜/旧房间/买车/接机/送别/打针/手术…）
- 同一情节簇出现 >2 场 = 报错
- 每类情节只允许保留1-2场，其余删除或改写为全新情节

### 闸门4：人物与事件一致性

- 人物年龄 = 出生年份差（未到生日不增岁）
- "X年前/后"与实际事件日期核对
- 车辆可用性：某车在某时间点必须可驾驶
- 同一人物离开时间在所有场次中一致
- "第一次XX"全片只出现一次

### 闸门5：格式与字数

- 每场四字段齐全：大纲锚点（作者口径）/ 时间：/ 地点：/ 出场人物：
- 每集三字段齐全：集标题 / 本集概要（按作者更正口径）/ 本集场次
- 中文字符数达标
- 中文弯引号，无 ASCII 引号

## 情节级重复检测脚本（v6.0 核心新增，必须运行）

```python
import re
from collections import defaultdict

def plot_cluster_scan(text, max_per_cluster=2):
    """
    按情节关键词聚类，找出同一情节被拆成多场的问题。
    这是 v6.0 最关键的脚本——之前所有版本都漏了这个维度。
    """
    # 情节关键词簇：每个簇代表"同一类事件"
    clusters = {
        '买显示器': ['显示器', 'OLED', '27寸', '三屏'],
        '装中央空调': ['中央空调', '空调安装', '一拖五', '风管机'],
        '买新工作站': ['工作站', '新电脑', 'RTX PRO', '至强', 'NVLink'],
        '给黑客报喜': ['北极熊', '银星', '俄罗斯黑客', '朝鲜黑客', '报喜', '报平安'],
        '走进旧房间': ['旧房间', '小时候', '小学奖状', '小熊玩偶', '变形金刚'],
        '买硬盘': ['硬盘', '256T', '144TB', '企业级SSD'],
        '买尚界Z7': ['尚界', 'Z7', '鸿蒙智行', '提车', '华为纯电'],
        '买笔记本': ['笔记本', 'ThinkPad', 'MacBook', '便携电脑'],
        '买尚界/提车': ['提车', '4S店', '新车', '试驾'],
        '接送/送别': ['送到机场', '接站', '送别', '拥抱', '上飞机', '登机'],
        '打针/急救': ['注射', '针剂', '急救', '给便衣', '特种药品'],
        '手术/治疗': ['手术', '激光', '疤痕修复', '治疗', '复查'],
        '姑姑做饭': ['红烧肉', '姑姑做', '午饭', '晚饭', '做饭'],
    }

    # 解析场次
    scenes = []
    current_title = None
    current_content = []
    for line in text.split('\n'):
        if re.match(r'^第\d+场', line.strip()):
            if current_title:
                scenes.append((current_title, '\n'.join(current_content)))
            current_title = line.strip()
            current_content = []
        else:
            current_content.append(line)
    if current_title:
        scenes.append((current_title, '\n'.join(current_content)))

    # 聚类
    hits = defaultdict(list)
    for title, content in scenes:
        for cluster_name, keywords in clusters.items():
            if any(kw in content for kw in keywords):
                hits[cluster_name].append(title)

    # 报告
    issues = []
    for cluster_name, titles in hits.items():
        if len(titles) > max_per_cluster:
            issues.append(
                f"✗ 情节簇「{cluster_name}」出现 {len(titles)} 场（>{max_per_cluster}）：\n"
                + '\n'.join(f"    - {t}" for t in titles)
                + f"\n  → 只保留最详细的1-{max_per_cluster}场，其余删除或改写为全新情节"
            )
    return issues
```

## 工作流（精简版）

### 开工前
1. 写 `state_baseline.md`：场次编号表、人物位置表、车辆可用性表、关键情节节点表、集数编号表
2. 列出**本幕不会写的情节**（防止后面为凑字数重写）

### 分批写
- 每批 ≤4000字
- 每批写完跑5个闸门
- 闸门不过就重写该批

### 交付前
1. 全量跑 `plot_cluster_scan`——这是 v6.0 新增的最关键一步
2. 全量跑 `verify_scene_order`
3. 全量跑 `detect_degradation`
4. 检查中文弯引号（无 ASCII "）
5. 字数达标
6. 另存新文件交付

## 退化模式速查（禁止）

| 错误 | 正确 |
|------|------|
| 裴砚，说 | 裴砚说 |
| 也可能，是，老挝警察 | 也可能是老挝警察 |
| 他，的心跳，加速 | 他心跳加速 |
| "明白。"钢哥，点头 | "明白。"钢哥点头 |

## 剧本格式

主场次：`第XX场`
补充场：`第XX场补充场（一）`（同一主场次下按一二三...连续）

每场四字段：
```
第341场
大纲锚点（作者口径）：一句话
时间：2033年11月2日 08:00
地点：贵阳，徐家老屋
出场人物：徐萱、姑姑
```

每集三字段：
```
第三季·第1集《集名》
本集概要（按作者更正口径）：一句话
本集场次：第341场—第350场
```

## 更新日志

### v6.0.0（2026-09）
- **新增情节级重复检测脚本** `plot_cluster_scan()`：按情节关键词聚类，同一情节簇>2场即报错。这是之前所有版本最严重的缺失——导致"买显示器写8场、装空调写7场、买工作站写17场"这类问题。
- **新增 ASCII 引号硬规则**：中文段落禁止 ASCII 双引号，必须用中文弯引号 U+201C/U+201D。
- **新增主场次删除后重编号规则**：删除主场次后必须重新编号，不得出现编号断裂。
- **新增"凑字数禁止重复"铁律**：字数不够时必须补充新情节类型，不得换皮重写已有情节。
- **精简规则**：从30+条检查清单压缩为5个硬闸门，每个闸门有明确脚本可跑。
- **修复格式错误**：清理破损表格行、重复编号、重复检查项。
- **压缩更新日志**：v1-v5 的详细日志压缩为一行。

### v5.0.0
- 工程化防退化体系：状态基线表 + 自动校验脚本 + 全量扫描闭环。

### v4.x
- v4.6：事件因果链一致性检测
- v4.4：送别情节去重、人物去向一致性
- v4.3：模板化重复场次检测
- v4.2：人物年龄精确到生日
- v4.1：场次四字段/集头三字段强制
- v4.0：集数标注、日本学制、年龄-年级对应
